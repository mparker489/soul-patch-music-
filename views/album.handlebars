<!DOCTYPE html>
<html>
<main id="albumPage">
    <div id="complete">
        <div id="photo">
            <img src="{{coverArt}}" class="albumBodyAlbum" />
        </div>
        <div id="description">
            <h1 class="albumBodyTitle">{{title}} by {{artist}}</h1>
            <p class="albumBodyDescription">{{description}}</p>
            <aside class="album-dropdown">
                <button type="button" class="buyButton"> Vinyl: $9.99 </button>
                <button type="button" class="buyButton"> Cassette: $2.00 </button>
                <button type="button" class="buyButton"> CD: $2.00 </button>
            </aside>
        </div>
    </div>
    
</main>

</html>